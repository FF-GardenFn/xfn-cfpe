# P-LEDGER prediction registry

One file per round, named `NNN-<slug>.md`, following [TEMPLATE.md](./TEMPLATE.md). The prediction commit's hash
is the round's timestamp; predictions are immutable after commit — corrections go in the scoring section, never
in the prediction.

**Running score:** 0 rounds · 0 hits · calibration: no data.

Survivorship caveat (travels with every score): published postmortems are successes, so this track record is
performance on the survivor distribution.
